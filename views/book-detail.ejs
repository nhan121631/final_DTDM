<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Chi Tiết Sách - <%= book.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4">
                <img src="<%= book.coverImage %>" class="img-fluid rounded shadow" alt="Bìa sách">
            </div>
            <div class="col-md-8">
                <h2 class="fw-bold"><%= book.title %></h2>
                <p class="text-muted">Tác giả: <strong><%= book.author ? book.author.name : "Không rõ" %></strong></p>
                <p class="text-muted">Thể loại: <strong><%= book.genre ? book.genre.name : "Không rõ" %></strong></p>
                <p class="text-muted">Năm xuất bản: <strong><%= book.year || "Không rõ" %></strong></p>
                <p class="mt-3"><%= book.description || "Không có mô tả" %></p>
                <a href="/edit-book/<%= book._id %>" class="btn btn-warning mt-3">Sửa</a>
                <a href="/delete-book/<%= book._id %>" class="btn btn-danger mt-3">Xóa sách</a>
                <a href="/" class="btn btn-secondary mt-3">Quay lại</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
